[project]
name = "squirrelfeeder-pi-zero"
version = "0.1.0"
description = "Raspberry Pi Zero W Media Pipeline for SquirrelFeeder"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["..", "."]
testpaths = ["tests"]
